import type { Meta, StoryObj } from "@storybook/web-components";

import "../CRUD";

const meta: Meta = {
  title: "7GUIs/CRUD",
  component: "crud-element",
  tags: ["autodocs"],
};

export default meta;
type Story = StoryObj;

export const Primary: Story = {};
