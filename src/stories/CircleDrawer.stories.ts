import type { Meta, StoryObj } from "@storybook/web-components";

import "../CircleDrawer";

const meta: Meta = {
  title: "7GUIs/CircleDrawer",
  component: "circle-drawer",
  tags: ["autodocs"],
};

export default meta;
type Story = StoryObj;

export const Primary: Story = {};
